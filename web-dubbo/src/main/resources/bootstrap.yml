server:
  port: 8081

spring:
  application:
    name: cmcc-web-dubbo
  main:
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      server-addr: localhost:8848
      username: nacos
      password: nacos
